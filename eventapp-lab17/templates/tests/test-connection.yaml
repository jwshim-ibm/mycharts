apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventapp-lab17.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "eventapp-lab17.name" . }}
    helm.sh/chart: {{ include "eventapp-lab17.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eventapp-lab17.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
